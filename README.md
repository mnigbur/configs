# configs

```
sh -c "$(wget https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)"
git clone https://github.com/mnigbur/linux-configs.git
ln -s linux-configs/.zshrc .zshrc
ln -s linux-configs/.screenrc .screenrc
```` 
